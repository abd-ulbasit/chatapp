{
    "name":"server",
    "main": "./server/server.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build": "cd server   && pwd && echo \" in the server directory now\" "
    }
}