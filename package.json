{
    "name":"server",
    "main": "./server/server.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start": "cd server  && npm i && echo \" in the server directory now\" && npm start"
    }
}